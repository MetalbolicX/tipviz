# Implementation Plans

Generated by the `improve` skill on 2026-08-13, stamped at commit `89420c8`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

## Discipline map (SDD vs strict TDD)

| Plan | Discipline | Why |
|------|-----------|-----|
| 001 TDD foundation   | **SDD** (policy spec) + **strict TDD** (characterization tests) | The testing *policy* is a design decision; the characterization tests are the first TDD output |
| 002 Sanitizer fix    | **strict TDD** | Concrete defect with observable behavior — red tests (from 001) → green |
| 003 Lint baseline    | **neither** — mechanical chore, lint is the gate | Not behavior to design/test; one recorded decision (align code to rules) |
| 004 AGENTS.md        | **neither** — documentation | No behavior, no tests; verified by grep/file-existence |
| 005 God-file split   | **SDD** (boundary design) **then strict TDD** (characterize + refactor) | Must design module contracts before moving code; tests prove behavior is preserved |

**Rule of thumb used here:** *SDD* when a change needs an agreed
specification/design before implementation (policy, architecture, public
boundaries); *strict TDD* when a change is concrete behavior you can pin with a
failing test first; *direct* for mechanical/docs work that has no behavior to
design or test.

## What to do first

1. **Start with 001.** It is the prerequisite — it creates the test+lint+typecheck
   gate and the characterization tests that every later plan relies on. Nothing
   else should touch `tooltip.mts` before 001 lands.
2. **Then 002** (sanitizer) — it turns 001's intentionally-red sanitizer tests
   green. Must follow 001.
3. **003 (lint) and 004 (AGENTS.md) can run in parallel** with each other and
   alongside 002; 003 needs the `pnpm run lint` script from 001, 004 is fully
   independent.
4. **005 (god-file split) is last.** It needs 001's tests and should run *after*
   002 (so the extracted sanitizer has correct behavior) and ideally after 003
   (so new modules are lint-clean from birth).

## Execution order & status

| Plan | Title | Discipline | Priority | Effort | Depends on | Status |
|------|-------|-----------|----------|--------|------------|--------|
| 001  | Establish strict-TDD foundation (policy + test gate) | SDD + TDD | P1 | M | — | DONE |
| 002  | Fix the sanitizer and correct its docs | strict TDD | P1 | S | 001 | DONE |
| 002b | Add native-DOM-Sanitizer type augmentation | direct | P2 | XS | 002 | DONE |
| 003  | Make the code obey its own lint rules | direct | P2 | S | 001 | TODO |
| 004  | Correct AGENTS.md to match v3.0 | direct | P2 | S | — | TODO |
| 005  | Decompose the god class (sanitizer + positioner) | SDD then TDD | P2 | M | 001, 002 (ideally 003) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **002 requires 001** — 001 writes the red sanitizer tests that 002 makes green.
- **003 requires 001** — uses the `pnpm run lint` script and the CI workflow 001 adds.
- **005 requires 001** (characterization tests) **and 002** (so extracted sanitizer is
  correct). 003 is recommended first so the extracted modules are lint-clean, but
  not strictly required.
- **004 is independent** — can land any time; pair it with 001 so the agent guide
  stops lying about "No tests" ASAP.

## Findings considered and rejected

- **D4 (`no-auto-reposition` not handled post-connect):** minor correctness bug,
  LOW-MED impact. Not worth a standalone plan; fold the fix into 005 or a future
  lifecycle-cleanup change. Record: it's a one-line `attributeChangedCallback`
  branch + test.
- **P3 (`loadStylesheet` no URL scheme allowlist):** LOW severity — browsers
  block `javascript:` on stylesheet links, so the real risk is tiny. Not worth a
  plan now; revisit if the sanitizer scope expands to URL attributes generally
  (it does in Plan 002, but for template HTML, not stylesheet URLs).
- **Major-version dependency lag:** not assessed as actionable — TypeScript 7 /
  Vite 8 / Vitest 4 are current at the time of writing per `package.json`.
